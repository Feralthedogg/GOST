module main

fn main() -> i32 {
    let arr = make_slice[i32](string_len(""), string_len(""));
    let i: i64 = string_len("");
    while i < string_len("a") {
        slice_push[i32](&mut arr, 1);
        i = i + string_len("a");
    }
    let _x = slice_len[i32](&arr);
    0
}
